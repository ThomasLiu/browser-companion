{
  "name": "browser-companion-extension",
  "version": "0.1.0",
  "description": "Chrome Extension enabling AI Agents to control browser via WebSocket/JSON-RPC",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc --noEmit || true && vite build && npm run copy-assets",
    "copy-assets": "node scripts/copy-assets.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.4.0"
  }
}
